# ===================================
# Spring Profile
# ===================================
SPRING_PROFILES_ACTIVE=your_profile_here

# ===================================
# Supabase Database Configuration
# ===================================
# Get these from: Supabase Dashboard > Project Settings > Database
# Connection Info > Connection String > URI (Transaction Mode recommended)

DB_HOST=db.xxxxxxxxxxxxx.supabase.co
DB_PORT=6543
DB_NAME=postgres
DB_USER=postgres.xxxxxxxxxxxxx
DB_PASSWORD=your_secure_password_here

# ===================================
# Redis Configuration
# ===================================
# LOCAL DEVELOPMENT:
#   - Uses Docker Redis (see docker-compose.local.yml)
#   - Set REDIS_PASSWORD=devPassword123
#   - No REDIS_HOST/PORT needed (defaults to localhost:6379)
#
# PRODUCTION:
#   - Uses Upstash Redis (managed service)
#   - Set all three values from Upstash dashboard
REDIS_HOST=your-upstash-url.upstash.io
REDIS_PORT=6379
REDIS_PASSWORD=your_upstash_password_here

# ===================================
# JWT Secret
# ===================================
JWT_SECRET=your_secure_jwt_secret_min_256_bits

# ===================================
# CORS Configuration
# ===================================
CORS_ALLOWED_ORIGINS=https://your-production-domain.com

# ===================================
# Security Notes
# ===================================
# 1. NEVER commit the .env file to version control
# 2. Use Transaction Mode (port 6543) for better connection pooling
# 3. Use different credentials for local/production
